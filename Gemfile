source "https://rubygems.org"

# Pin local builds to the same stack GitHub Pages uses (see https://pages.github.com/versions/).
gem "github-pages", group: :jekyll_plugins

gem "wdm", "~> 0.1.0" if Gem.win_platform?
